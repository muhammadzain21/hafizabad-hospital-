import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

const IpdSummary = () => {
  return (
    <div className="p-4">
      <Card>
        <CardHeader>
          <CardTitle>IPD Summary</CardTitle>
        </CardHeader>
        <CardContent>
          <p>This is the IPD Summary page. Content will be added here soon.</p>
        </CardContent>
      </Card>
    </div>
  );
};

export default IpdSummary;
