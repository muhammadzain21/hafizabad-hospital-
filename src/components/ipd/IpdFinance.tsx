import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

const IpdFinance = () => {
  return (
    <div className="p-4">
      <Card>
        <CardHeader>
          <CardTitle>IPD Finance</CardTitle>
        </CardHeader>
        <CardContent>
          <p>This is the IPD Finance page. Content will be added here soon.</p>
        </CardContent>
      </Card>
    </div>
  );
};

export default IpdFinance;
